% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_arrows.R
\name{add_arrows}
\alias{add_arrows}
\title{Add directional arrow symbols to a column based on comparisons}
\usage{
add_arrows(
  .data,
  column,
  compare_to = NULL,
  baseline = NULL,
  new_col = NULL,
  direction = c("updown", "leftright"),
  same_symbol = "•"
)
}
\arguments{
\item{.data}{A data frame or tibble.}

\item{column}{The column to evaluate (unquoted).}

\item{compare_to}{(Optional) A second column to compare against (unquoted).}

\item{baseline}{(Optional) A numeric constant to compare against.}

\item{new_col}{(Optional) Name of the new column to add. Defaults to `column_arrow`.}

\item{direction}{Character; either `"updown"` for ▲▼ or `"leftright"` for ▶◀. Default is `"updown"`.}

\item{same_symbol}{Character symbol to use when values are equal. Default is a medium circle.}
}
\value{
A data frame with an additional column of arrow symbols.
}
\description{
Appends a new column with directional symbols (arrows) indicating whether values
in a target column are higher, lower, or equal to a baseline or another column.
For left and right arrows works best if using Source Sans Pro font.
}
\details{
Useful for dashboards, tables, or data exports that visually signal changes or trends.
}
\examples{
\dontrun{
library(dplyr)

df <- tibble::tibble(
  current = c(90, 75, 80),
  previous = c(85, 80, 80)
)

# Compare to another column
add_arrows(df, current, compare_to = previous)

# Compare to fixed baseline
add_arrows(df, current, baseline = 80)

# Custom symbol and direction
add_arrows(df, current, baseline = 80, direction = "leftright", same_symbol = "•")
}

}
