% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_helpers.R
\name{drkn_clmn_hdr}
\alias{drkn_clmn_hdr}
\title{Style column headers in a gt table}
\usage{
drkn_clmn_hdr(.data, color = "#414042")
}
\arguments{
\item{.data}{A `gt` table object.}

\item{color}{A color to apply to the column header text. Defaults to `gt::grey90k`.
You can use any valid color string (e.g., `"black"`, `"#333333"`).}
}
\value{
A `gt` table object with styled column labels.
}
\description{
Applies custom text color to all column headers in a `gt` table using `gt::tab_style()`.
By default, it uses a high-contrast dark gray, but you can pass any valid
R color name or hex code.
}
\examples{
\dontrun{
library(gt)

# Use default dark gray
gt_tbl <- gt::gt(head(mtcars))
drkn_clmn_hdr(gt_tbl)

# Use a custom color
drkn_clmn_hdr(gt_tbl, color = "blue")
drkn_clmn_hdr(gt_tbl, color = "#336699")
}

}
