% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_arrows.R
\name{add_arrow_column_gt}
\alias{add_arrow_column_gt}
\title{Add a directional arrow column to a gt table}
\usage{
add_arrow_column_gt(
  data,
  column,
  compare_to = NULL,
  baseline = NULL,
  direction = c("updown", "leftright"),
  same_symbol = "",
  up_color = "#67A784",
  down_color = "#E571B0",
  new_col_name = "delta"
)
}
\arguments{
\item{data}{A data frame to render as a `gt` table.}

\item{column}{The numeric column to annotate with arrows (unquoted or quoted).}

\item{compare_to}{A column to compare against (unquoted or quoted). Use for row-wise comparison.}

\item{baseline}{A numeric value or vector (used if `compare_to` is NULL).}

\item{direction}{`"updown"` (▲▼) or `"leftright"` (▶◀). Default is `"updown"`.}

\item{same_symbol}{Symbol for equal values. Default is `""`.}

\item{up_color}{Color for upward arrows. Default is `"forestgreen"`.}

\item{down_color}{Color for downward arrows. Default is `"firebrick"`.}

\item{new_col_name}{Optional name for the new arrow column. Default is `"delta"`.}
}
\value{
A styled `gt` table with a new arrow column.
}
\description{
Adds a new column (default `"delta"`) containing directional arrows (▲▼ or ▶◀),
based on comparisons to another column or numeric baseline. Arrows are color-coded and centered.
}
\examples{
\dontrun{
df <- tibble::tibble(
  region = c("A", "B", "C"),
  score = c(90, 85, 92),
  goal = c(88, 88, 90)
)

add_arrow_column_gt(df, column = score, compare_to = goal)

add_arrow_column_gt(df, column = score, compare_to = goal, new_col_name = "change")
}

}
