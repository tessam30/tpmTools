% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_functions.R
\name{rate_hmis}
\alias{rate_hmis}
\title{Rate HMIS Indicator Performance}
\usage{
rate_hmis(
  .data,
  hmis_col,
  label_col = "hmis_label",
  color_col = "hmis_color",
  threshold = 0.85,
  label_optimal = "Optimal",
  label_failure = "Failure",
  color_optimal = "#67A784",
  color_failure = "#E571B0"
)
}
\arguments{
\item{.data}{A data frame or tibble containing the HMIS indicator column.}

\item{hmis_col}{Unquoted column name representing the HMIS indicator (numeric values between 0 and 1).}

\item{label_col}{A string giving the name of the new column to hold the performance classification label. Defaults to `"hmis_label"`.}

\item{color_col}{A string giving the name of the new column to hold the color associated with each performance class. Defaults to `"hmis_color"`.}

\item{threshold}{Numeric threshold above which the indicator is considered "Optimal". Defaults to `0.85`.}

\item{label_optimal}{Label assigned to values above the threshold. Defaults to `"Optimal"`.}

\item{label_failure}{Label assigned to non-missing values below or equal to the threshold. Defaults to `"Failure"`.}

\item{color_optimal}{Hex color code for optimal performance. Defaults to `"#67A784"` (green).}

\item{color_failure}{Hex color code for failure. Defaults to `"#E571B0"` (pink).}
}
\value{
A modified version of `.data` with two new columns: one for labels (`label_col`) and one for color codes (`color_col`).
}
\description{
This function evaluates the performance of a numeric HMIS (Health Management Information System) indicator
against a specified threshold and assigns corresponding labels and colors. It is intended for use in
performance reporting or dashboards to quickly classify results as "Optimal" or "Failure".
}
\examples{
\dontrun{
library(dplyr)
df <- tibble::tibble(hmis_score = c(0.9, 0.8, NA))
rate_hmis(df, hmis_score)
}
}
